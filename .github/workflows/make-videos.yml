name: Make & Upload Forex Videos (FREE)

on:
  schedule:
    - cron: '5 * * * *'   # run hourly (UTC)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # allow the script to commit posted_state.json

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system tools (FFmpeg)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install moviepy Pillow google-api-python-client google-auth google-auth-oauthlib requests pandas pytz

      - name: Run generator
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
        run: python make_videos.py

      - name: Upload renders as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renders
          path: renders/**/*.mp4
          retention-days: 3
